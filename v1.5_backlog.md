# v1.5 Backlog

Items deferred from v1 scope. Any new "let's also add X" idea must land here first before discussion.

Source: design Section 10 (`docs/plans/2026-05-11-enterprise-knowledge-agent-design.md`).

## Deferred from v1 (post-audit and Codex review)

- **AWS ECS Fargate deploy** (post-audit downgrade from v1). Terraform / CDK; S3 + RDS + Secrets Manager; real AWS signal.
- **similar_briefing_history tool** (post-audit downgrade from v1 W3). Historical briefing RAG + cross-source decision context.
- **Real Okta / Azure AD federation** (post-audit RBAC upgrade). SAML / OIDC + cross-source identity linking; land the policy engine pattern onto real federation.
- **More external benchmarks** (post-audit eval depth). BEIR full suite, MuSiQue multi-step QA, TREC-RAG 2024-2025.
- **MCP server full integration** (v1.4 roll back; "1 MCP wrapper" not meaningful). v1.5 stack-wide MCP: 6 tools as MCP servers + real Slack / Notion / Confluence / Jira / GitHub replacing mocks (8-12h).
- **Llama 3.3 70B local complete ablation** (v1.4.2 pre-emptive move from v1 stretch). Ollama + full leaderboard column vs Sonnet / GPT-4o / Haiku cost/quality trade-off.
- **Cryptographic audit hash chain**. Attestation per EU AI Act compliance.

## Added during v1 execution (post-W4)

- **Multi-LLM ablation as second / third leaderboard column** — v1 ships with DeepSeek V4 Pro only per W2 decision. Once an Anthropic key arrives, add a Sonnet 4.6 column; once an OpenAI key arrives, add GPT-4o. The eval harness already takes a model identifier; only the LLM client wrapper needs branch logic.
- **HotpotQA proper QA chain** — v1 uses a naive sentence-overlap span extractor for HotpotQA, which lands at F1 0.077. Swap for an LLM-driven answer step using the same retrieval primitive; expected F1 ~0.55-0.70.
- **PostgreSQL audit log** — v1 ships JSONL via `AUDIT_LOG_PATH`. Schema + migration scripts pending.
- **External reviewer spot-check** — invite 1-2 NL tech contacts to score 5-10 scenarios independently. Surface the discrepancy in the methodology blog.
- **Fly.io / HF Spaces deploy** — `infra/Dockerfile` ready; `fly.toml` + `huggingface-space.yml` pending.
- **Demo 2 (HR Helpdesk single-source modularity case study)** — optional 2-day case study per design. Module reuse table is already in `docs/architecture.md`; the actual three tools + HR corpus stay pending.
- **Demo video recording** — `docs/demo-script.md` is the storyboard. Loom or OBS capture pending.

## Other v1.5 items

- Multimodal (chart / screenshot understanding as cross-source evidence).
- Multi-turn conversation + memory (multi-round followup decisions).
- Real customer dogfood (invite NL tech contacts to import sample data).
- A/B test framework for agent prompt versions.
- Auto-action execution (closed loop: auto-reschedule meetings, auto-reply email with approval). Currently recommend-only.
- Self-improving: feed user feedback back into RAG knowledge base.
- Adversarial prompt injection auto-red-team suite (CI-integrated, cross-source attack vectors).
- Real corporate Slack / Microsoft Teams via bot.
- Multilingual (NL Dutch + EN + other EU languages cross-source).
- Multi-tenant workspace isolation (commercial path).
